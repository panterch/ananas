= simple_form_for [parent, resource] do |f|
  .row= f.association :team
  .row= f.association :member
  = link_to 'create new member', new_form_team_team_members_path, remote: true

  .row= f.input :active

  = f.button :submit