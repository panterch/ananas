= simple_form_for resource do |f|
  .row= f.input :name
  .row= f.input :description

  .row= f.input :avatar

  = render 'vcards/form', f: f

  .row
    .col.s12= f.button :submit

= render 'child_list', association: :members
= render 'child_list', association: :mentors
