= simple_form_for resource do |f|
  .section
    .row= f.input :name
    .row= f.input :description
    .row.input-field.col.s12
      = f.label :avatar, class: 'file-field input-field'
      .row.col
        = f.input :avatar, as: :image_preview, label: 'upload avatar', label_html: { class: 'waves-effect waves-light btn waves-input-wrapper' }
    .row
      %img.col.s3#img_prev{alt: "no avatar uploaded yet", src: resource.avatar.url }/

    = render 'vcards/form', f: f, full_name: true

    .row
      .col.s12= f.button :submit

- if resource.persisted?
  .section_divider
  .section
    .header
      %h4= t_title(:index, Mentoring)
      %div(style="position: relative")
        .fixed-action-btn.horizontal(style="position: absolute")
          = link_to new_team_mentoring_path(resource), remote: true, class: "btn-floating btn-large blue" do
            %i.large.material-icons add
      = render 'events/list', collection: resource.mentorings.upcoming, resource_class: Mentoring

  .section_divider
  .section
    .header
      %h4= t_title(:index, Member)
      %div(style="position: relative")
        .fixed-action-btn.horizontal(style="position: absolute")
          = link_to new_team_team_member_path(resource), remote: true, class: "btn-floating btn-large blue" do
            %i.large.material-icons add
      = render 'members/list', collection: resource.members

  .section_divider
  .section
    .header
      %h4= t_model Mentor
      %div(style="position: relative")
        .fixed-action-btn.horizontal(style="position: absolute")
          = link_to new_team_team_mentor_path(resource), remote: true, class: "btn-floating btn-large blue" do
            %i.large.material-icons add
      = render 'mentors/list', collection: resource.mentors
