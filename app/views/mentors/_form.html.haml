= simple_form_for resource do |f|
  = f.input :job_title
  = f.input :who_i_am
  = f.input :experience
  = f.input :interests
  = f.input :motivation

  = render 'vcards/form', f: f

  = f.button :submit, class: 'btn-primary'

= render 'child_list', association: :teams
